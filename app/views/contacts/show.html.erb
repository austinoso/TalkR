<h1>Contact: <%= @contact.name %></h1>
<%= button_to "Remove From Contacts", contact_path(@contact), method: :delete, data: { confirm: "Are you sure you want to remove this person from your contacts?" } %>